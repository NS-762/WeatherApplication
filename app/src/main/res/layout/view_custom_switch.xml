<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.example.weatherapplication.ui.view.CustomSwitchItem
        android:id="@+id/first_item"
        android:layout_width="70dp"
        android:layout_height="30dp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="@color/background_3"
        tools:text="Да"
        tools:textColor="@color/white" />

    <com.example.weatherapplication.ui.view.CustomSwitchItem
        android:id="@+id/second_item"
        android:layout_width="70dp"
        android:layout_height="30dp"
        android:gravity="center"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/first_item"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="@color/argent"
        tools:text="Нет"
        tools:textColor="@color/sonic_silver" />

</androidx.constraintlayout.widget.ConstraintLayout>